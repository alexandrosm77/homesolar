services:
  homesolar:
    image: ${HOMESOLAR_IMAGE:-homesolar:local}
    build:
      context: .
      target: production
    container_name: homesolar
    restart: unless-stopped
    ports:
      - "${HOMESOLAR_PORT:-8080}:8000"
    environment:
      HOMESOLAR_WEB_USER: ${HOMESOLAR_WEB_USER:-}
      HOMESOLAR_WEB_PASSWORD: ${HOMESOLAR_WEB_PASSWORD:-}
      REMOTE_INVERTER_USER: ${REMOTE_INVERTER_USER:-}
      REMOTE_INVERTER_PASSWORD: ${REMOTE_INVERTER_PASSWORD:-}
    volumes:
      - ${HOMESOLAR_CONFIG:-./config/local.yaml}:/config/homesolar.yaml:ro
      - ${HOMESOLAR_DATA_DIR:-./data}:/app/data
